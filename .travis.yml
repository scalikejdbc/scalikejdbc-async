language: scala
dist: xenial
jdk:
- openjdk8
- openjdk11
scala:
- 2.13.3
- 2.12.12
sudo: false
script:
- sbt "++ ${TRAVIS_SCALA_VERSION}!" test
- git diff --exit-code # check scalariform
matrix:
  include:
  - name: Scala3
    script:
    - sbt "++ 3.0.0-M1!" compile
before_script:
- rm project/pgp.sbt && rm project/sbt-updates.sbt && rm project/sonatype.sbt
- git add . --all
